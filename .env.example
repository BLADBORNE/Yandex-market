# Development-only credentials for the local Docker Compose environment.
# Copy this file to .env and replace both values before starting the stack.
KEYCLOAK_ADMIN_PASSWORD=change-this-admin-password
PAYMENT_OAUTH_CLIENT_SECRET=change-this-market-client-secret
